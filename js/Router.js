export class Router {
    constructor(RID, AID) {
        this.AID = AID;
        this.RID = RID;
        this.LSDB = new Array();
    }
    // Getter
    
    // Setter

    // Method
    helloMsg() {
        
    }
}